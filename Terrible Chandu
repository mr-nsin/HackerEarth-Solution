#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main()
{
	int t,s_len;
	string str,r_str;
	cin>>t;
	while(t--){
		cin>>str;
		/*s_len = str.length();
		for(int i =s_len-1;i>=0;i--){
			cout<<str[i];
		}*/
		reverse(str.begin(),str.end());
		
		cout<<str<<"\n";
	}
	
    return 0;
}
